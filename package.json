{
  "name": "webreed-standard-generator",
  "version": "0.0.1",
  "description": "Standard generator plugin for webreed.",
  "main": "lib/setup.js",
  "scripts": {
    "prepublish": "babel src --out-dir lib",
    "devmode": "babel --watch src --out-dir lib",
    "test": "mocha ./test/ --recursive --compilers js:babel-register"
  },
  "author": "Rotorz Limited",
  "license": "MIT",
  "dependencies": {
    "rxjs": "^5.0.0-beta.2",
    "webreed-core": "^0.0.1"
  },
  "devDependencies": {
    "babel-cli": "^6.5.1",
    "babel-preset-node5": "^10.6.0",
    "babel-register": "^6.5.2",
    "mocha": "^2.3.4",
    "mocha-testdata": "^1.1.2",
    "should": "^8.0.2"
  }
}
